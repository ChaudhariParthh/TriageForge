I have a security alert coming into n8n. I need to extract useful IOCs from the alert, such as IP addresses, URLs, domains, and file hashes.

Write JavaScript for an n8n Code node that extracts these values, removes duplicates, and keeps the original alert data along with the extracted IOCs.