I now have the original alert combined with results from VirusTotal, AbuseIPDB, GeoIP, and AlienVault OTX.

Create JavaScript for an n8n Code node that brings the important evidence together. It should include failed login attempts, threat-intelligence results, network context, and a count of how many threat-intelligence sources show a malicious signal.

The output should be clean and easy for the next risk-scoring step to use.